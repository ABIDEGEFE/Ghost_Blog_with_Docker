const audio = document.getElementById("audioPlayer");

function setSpeed(rate) {
    if(audio){
        audio.playbackRate = rate;
    }
}
console.log("working..")

const audio = document.getElementById("audioPlayer");

function setSpeed(rate) {
    if(audio){
        audio.playbackRate = rate;
    }
}
console.log("working..")

const audio = document.getElementById("audioPlayer");

function setSpeed(rate) {
    if(audio){
        audio.playbackRate = rate;
    }
}
console.log("working..")

const audio = document.getElementById("audioPlayer");

function setSpeed(rate) {
    if(audio){
        audio.playbackRate = rate;
    }
}
console.log("working..")