apiVersion: v1
kind: Service
metadata:
  name: caddy-service
  namespace: cms-iam
spec:
  type: NodePort
  selector:
    app: caddy-gateway
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: "node-port" # Matching your current setup
